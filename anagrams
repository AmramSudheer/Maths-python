class An:
    def anagram(s,t):
        s = input("enter the string : ")
        t = input("enter the string : ")
        if (sorted(s)==sorted(t)):
            return True
        else:
            return False
    print(anagram("",""))
